const inputElement = document.getElementById('input');
const listContainerElement = document.getElementById('list-container');
const buttonClearMobileElement = document.getElementById(
	'clear-completed-mobile'
);
const buttonClearElement = document.getElementById('clear-completed');
const infoElement = document.getElementById('show-info');
const nightElement = document.getElementById('night-mode');
const rootStyles = document.documentElement.style;

export {
	inputElement,
	listContainerElement,
	buttonClearMobileElement,
	buttonClearElement,
	infoElement,
	rootStyles,
	nightElement
};
